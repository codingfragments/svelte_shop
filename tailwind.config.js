/** @type {import('tailwindcss').Config} */
export default {
	content: ['./src/**/*.{html,js,svelte,ts}'],
	theme: {
		extend: {
			// All theme customizations now moved to CSS using @theme
		}
	},
	plugins: []
};